# Task 1

1. Run the program `crash.py`.

2. Compile and run the program `crash.c`.

3. Write down your observations of how the programs behave in a new
   Markdown file named `notes.md`. the debugger pf 

   Include a comment in `notes.md` on which of these programs will be easier
   to debug, based on your observations. Explain your reasoning.
   crash.py will be easier to debug because it clearly states the error - IndexError: list assignment index out of range which is the exact same error as the one in the c file but it is more detailed, gets straight to the point and i don't have to take any inferences as to what the error could be like with the c file.

*** stack smashing detected ***: terminated
Aborted (core dumped) is what is stated by crash.c